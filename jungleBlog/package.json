{
  "name": "jungleblog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "buildToBackLinux": "vite build && cp -r ./dist/* ../backend/static",
    "buildToBackWindows": "vite build && rmdir /s /q ..\\backend\\static && xcopy /s /e /h dist ..\\backend\\static"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.4.2",
    "svelte": "^4.0.5",
    "vite": "^4.4.5"
  }
}
